#!/usr/bin/env ruby

$LOAD_PATH << "/usr/lib"
require "cluster-element"
require "installer"

ClusterElement::Config.gems.map{|g|g.install}
ClusterElement::Config.apks.map{|a|a.install}
ClusterElement::Cli.start