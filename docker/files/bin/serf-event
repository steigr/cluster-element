#!/usr/bin/env ruby

$LOAD_PATH << File.expand_path("$0/../usr/lib")

if ENV["SERF_EVENT"] == "query"
  if ENV["SERF_QUERY_NAME"] == "etcd-discover-token"
    require "cluster-element"
    puts ClusterElement::Etcd.local_token
  end
end